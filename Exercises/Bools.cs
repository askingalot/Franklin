namespace Franklin.Exercises
{
    public class Bools
    {
        public bool Not(bool value)
        {
            return !value;
        }

        public bool And(bool a, bool b, bool c)
        {
            return a && b && c;
        }

        public bool Or(bool a, bool b)
        {
            return a || b;
        }
    }
}